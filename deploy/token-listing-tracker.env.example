# Receipt-bindable parent Telegram DM; thread targets are rejected when the
# installed Hermes version cannot prove the effective thread in its receipt.
LISTING_TRACKER_TARGET=telegram:<chat_id>
LISTING_TRACKER_TIMEZONE=Australia/Perth
