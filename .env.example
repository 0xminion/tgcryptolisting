# Copy to ~/.config/token-listing-tracker.env and replace <chat_id>.
# Use a parent Telegram chat while Hermes receipts do not expose thread identity.
LISTING_TRACKER_TARGET=telegram:<chat_id>
LISTING_TRACKER_TIMEZONE=Australia/Perth
